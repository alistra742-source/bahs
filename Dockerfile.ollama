# Use this Dockerfile for a dedicated Ollama service:
#   Railway -> service -> Settings -> Build -> Dockerfile Path = Dockerfile.ollama
# (or set RAILWAY_DOCKERFILE_PATH=Dockerfile.ollama)
# Mount that service's volume at /data so model weights persist across deploys.
FROM ollama/ollama:latest

ENV OLLAMA_HOST=0.0.0.0:11434 \
    OLLAMA_MODELS=/data/ollama \
    MODEL=qwen2.5-coder:3b

EXPOSE 11434

COPY entrypoint-ollama.sh /usr/local/bin/entrypoint-ollama.sh

# Base image ships ENTRYPOINT ["/bin/ollama"]; clear it before setting our own CMD.
ENTRYPOINT []
CMD ["sh", "/usr/local/bin/entrypoint-ollama.sh"]
